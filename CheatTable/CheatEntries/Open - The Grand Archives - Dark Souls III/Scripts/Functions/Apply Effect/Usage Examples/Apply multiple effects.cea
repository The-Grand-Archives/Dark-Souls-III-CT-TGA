{$lua}
if syntaxcheck then return end
[ENABLE]
local ids = {
    101310000,  -- Magic Weapon
    101330000,  -- Magic Shield
    101354000   -- Spook
}

for _,id in pairs(ids) do
    SpEffect.add(id)
end

disableMemrec(memrec)
[DISABLE]
